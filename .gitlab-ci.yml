stages:
  - push
  - build
  - start

pushToServer:
  stage: push
  image: ubuntu
  script: 
    - which ssh || apt update && apt install -y ssh

build: #job
  stage: build
  image: ubuntu
  script: 
    - echo "Hello world"